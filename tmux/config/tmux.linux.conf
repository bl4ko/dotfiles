set-option -g focus-events on
set-option -sg escape-time 10
set -g default-terminal "screen-256color"
bind-key -T copy-mode-vi MouseDragEnd0Pane send-keys -X copy-pipe-and-cancel 'xclip -in -selection clipboard'
